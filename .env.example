# Copy this file to .env and fill values before running the app
# Example for PostgreSQL (asyncpg):
# DATABASE_URL=postgresql+asyncpg://<username>:<password>@localhost:5432/<db_name>

# For local testing with a Postgres instance you can use:
# DATABASE_URL=postgresql+asyncpg://postgres:password@localhost:5432/bookly_db

# NOTE: your project uses PostgreSQL-specific column types (UUID, TIMESTAMP with timezone),
# so SQLite is not a drop-in replacement for production. Use Postgres to run migrations and the app.
